/*
    mUtils (created by miskaa)
    
    will be used by ../ui.hpp

    miskaas geode utils
*/

namespace mutils {
    namespace ui {
        namespace animation {
            namespace linear {
                static float in(float t) { return t; }
                static float out(float t) { return t; }
                static float inOut(float t) { return t; }
            }
        }
    }
}

// The Unlicense license (do anything you want with this code loll)